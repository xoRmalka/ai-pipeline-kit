# Context

Drop all assignment or brief files here before running `/analyze`.

Accepted formats: `.md`, `.pdf`, `.txt`, `.png`, `.jpg`, `.yaml`, or any file included in the task brief.

The `/analyze` command reads everything in this directory to produce `.ai/spec.md` and create the Linear task backlog.
